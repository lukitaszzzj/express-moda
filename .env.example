# Copiá este archivo como .env y pegá tu clave de Google Maps (Maps JavaScript API).
# Después corré: node scripts/maps-config.mjs
# En Vercel, cargá la misma variable en Settings > Environment Variables.
GOOGLE_MAPS_API_KEY=
